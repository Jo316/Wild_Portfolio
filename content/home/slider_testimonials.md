+++
# Slider widget.
widget = "slider"  # See https://sourcethemes.com/academic/docs/page-builder/
headless = true  # This file represents a page section.
active = true  # Activate this widget? true/false
weight = 100  # Order that this section will appear.

# Slide interval.
# Use `false` to disable animation or enter a time in ms, e.g. `5000` (5s).
interval = false

# Slide height (optional).
# E.g. `500px` for 500 pixels or `calc(100vh - 70px)` for full screen.
height = ""

# Slides.
# Slides 1
# Duplicate an `[[item]]` block to add more slides.
[[item]]
  title = ""
  content = "'Joseph was one of the top students in his year, and gained the highest individual mark on my course on flood hazards. He also worked within my research group as a summer intern looking at flood risks in the Congo basin.  He produced amazing work, far beyond what we expected, and we were able to publish the findings at a major conference in the US.' Professor Paul Bates, CBE, Professor of Hydrology at University of Bristol and Chairman of Fathom Global"
  align = "right"  # Choose `center`, `left`, or `right`.

  # Overlay a color or image (optional).
  #   Deactivate an option by commenting out the line, prefixing it with `#`.
  overlay_color = "#666"  # An HTML color value.
  overlay_img = "headers/bubbles-wide.jpg"  # Image path relative to your `static/img/` folder.
  overlay_filter = 0.5  # Darken the image. Value in range 0-1.

  # Call to action button (optional).
  #   Activate the button by specifying a URL and button label below.
  #   Deactivate by commenting out parameters, prefixing lines with `#`.
  # cta_label = "Get Academic"
  # cta_url = "https://sourcethemes.com/academic/"
  # cta_icon_pack = "fas"
  # cta_icon = "graduation-cap"

# Slides 2
[[item]]
  title = ""
  content = "'We were delighted to have Joseph over the summer and impressed with all the work that he did. He was a pleasure to work with. I hope that his studies goes well this year. We’d love to have him back at some point in the future.' Rob Porter, head of Digital Product at Canopius Group"
  align = "right"  # Choose `center`, `left`, or `right`.

  # Overlay a color or image (optional).
  #   Deactivate an option by commenting out the line, prefixing it with `#`.
  overlay_color = "#666"  # An HTML color value.
  overlay_img = "headers/bubbles-wide.jpg"  # Image path relative to your `static/img/` folder.
  overlay_filter = 0.5  # Darken the image. Value in range 0-1.

  # Call to action button (optional).
  #   Activate the button by specifying a URL and button label below.
  #   Deactivate by commenting out parameters, prefixing lines with `#`.
  # cta_label = "Get Academic"
  # cta_url = "https://sourcethemes.com/academic/"
  # cta_icon_pack = "fas"
  # cta_icon = "graduation-cap"
  
  
  # Slides 3
[[item]]
  title = ""
  content = "'Joseph helped build capacity within the team in terms of developing an evidence base for our Local Industrial Strategy, meaning our evidence could cover more breadth and depth than it otherwise would of. The evidence supporting the LIS has helped WECA in identify 4 themes under which policy will be developed. He also helped us explore new methods of data presentation through Tableau.' Charlotte Hopeley, Senior Economic Officer at WECA"
  align = "right"  # Choose `center`, `left`, or `right`.

  # Overlay a color or image (optional).
  #   Deactivate an option by commenting out the line, prefixing it with `#`.
  overlay_color = "#666"  # An HTML color value.
  overlay_img = "headers/bubbles-wide.jpg"  # Image path relative to your `static/img/` folder.
  overlay_filter = 0.5  # Darken the image. Value in range 0-1.

  # Call to action button (optional).
  #   Activate the button by specifying a URL and button label below.
  #   Deactivate by commenting out parameters, prefixing lines with `#`.
  # cta_label = "Get Academic"
  # cta_url = "https://sourcethemes.com/academic/"
  # cta_icon_pack = "fas"
  # cta_icon = "graduation-cap"
+++
